/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package dukapp.mobile.app;

import dukapp.mobile.list.LinkedList;

import static dukapp.mobile.utilities.StringUtils.join;
import static dukapp.mobile.utilities.StringUtils.split;
import static dukapp.mobile.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
